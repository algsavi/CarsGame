@page
@using CarsGame.Application;
@using CarsGame.Domain.Car;
@using CarsGame.Infrastructure;

@model CarsGame.WebApp.Pages.Game.GameModel
@inject CarService CarService

@{
    
    List<Car> cars = CarService.GetAll().ToList();
}

<div class="container-lg">
    <div class="table-responsive">
        <div class="table-wrapper">
            <div class="table-title">
                <div class="row">
                    <div class="col-sm-8"><h2>Cars</h2></div>
                    <div class="col-sm-4">
                        <a href="/Admin/Cars/Create" class="btn btn-primary" role="button">Add New Car</a>
                    </div>
                </div>
            </div>
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Make</th>
                        <th>Model</th>
                        <th>Year</th>
                        <th>Doors</th>
                        <th>Color</th>
                        <th>Price?</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var car in cars)
                    {
                        <tr>
                            <td>@car.Make</td>
                            <td>@car.Model</td>
                            <td>@car.Year</td>
                            <td>@car.Doors</td>
                            <td>@car.Color</td>
                            <td><a asp-page="GameTry" class="btn-sm btn-success" asp-route-id="@car.Id">Try</a></td>
                        </tr>
                    }      
                </tbody>
            </table>
        </div>
    </div>
</div>